<div class="w-full">
  <h1 class="font-semibold text-center text-3xl text-gray-800">Routine for <%= @prescription.full_name %></h1>

  <div class="mt-10 mx-auto max-w-md">
    <%= render "form",
      url: prescription_routine_path(@prescription),
      method: :post,
      submit_label: "Save routine" %>
  </div>
</div>
