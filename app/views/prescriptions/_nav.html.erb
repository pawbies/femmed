<nav class="mt-8 mb-12 flex gap-8 border-b border-gray-200 overflow-x-scroll">
  <%= link_to t(".log"), prescription_doses_path(@prescription),
      class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
        current_page?(prescription_doses_path(@prescription)) ? 'border-b-2 border-rose-500 text-rose-600' : ''
      }" %>

  <% if @prescription.pack_tracking_enabled? %>
    <%= link_to t(".packs"), prescription_packs_path(@prescription),
        class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
          current_page?(prescription_packs_path(@prescription)) ? 'border-b-2 border-rose-500 text-rose-600' : ''
        }" %>
  <% end %>

  <%= link_to t(".routine"), prescription_routine_path(@prescription),
      class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
        current_page?(prescription_routine_path(@prescription)) ? 'border-b-2 border-rose-500 text-rose-600' : ''
      }" %>

  <%= link_to t(".about"), prescription_about_path(@prescription),
      class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
        current_page?(prescription_about_path(@prescription)) ? 'border-b-2 border-rose-500 text-rose-600' : ''
      }" %>

  <%= link_to t(".settings"), edit_prescription_path(@prescription),
      class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
        current_page?(edit_prescription_path(@prescription)) ? 'border-b-2 border-rose-500 text-rose-600' : ''
      }" %>
</nav>

