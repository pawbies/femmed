<div class="w-full">
  <h1 class="text-3xl font-bold text-gray-900"><%= @user_medication.medication_version.full_name %></h1>

  <turbo-frame id="user_medications_page">
    <nav class="mt-8 mb-12 flex gap-8 border-b border-gray-200">
      <%= link_to "Log", user_medication_path(@user_medication, page: "Log"),
          class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
            params[:page].nil? || params[:page] == 'Log' ? 'border-b-2 border-rose-500 text-rose-600' : ''
          }" %>

      <%= link_to "Packs", user_medication_path(@user_medication, page: "Packs"),
          class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
            params[:page] == 'Packs' ? 'border-b-2 border-rose-500 text-rose-600' : ''
          }" %>

      <%= link_to "Settings", user_medication_path(@user_medication, page: "Settings"),
          class: "pb-3 font-medium text-gray-600 hover:text-gray-900 transition-colors #{
            params[:page] == 'Settings' ? 'border-b-2 border-rose-500 text-rose-600' : ''
          }" %>
    </nav>

    <% if params[:page] == "Log" %>
      <%= render "user_medications/pages/log" %>
    <% elsif params[:page] == "Packs" %>
      <%= render "user_medications/pages/packs" %>
    <% elsif params[:page] == "Settings" %>
      <%= render "user_medications/pages/settings" %>
    <% else %>
      <%= render "user_medications/pages/log" %>
    <% end %>
  </turbo-frame>
</div>
