<!DOCTYPE html>
<html lang="<%= I18n.locale %>" class="no-js">
  <head>
    <title><%= content_for(:title) || "FemMed" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Femmed">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <meta name="author" content="Alexander Fuchs">
    <meta name="description" content="Medication tracking system">

    <meta property="og:title" content="FemMed">
    <meta property="og:description" content="Medication tracking system">
    <meta property="og:image" content="/icon.svg">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:type" content="website">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="FemMed">
    <meta name="twitter:description" content="Medication tracking system">
    <meta name="twitter:image" content="/icon.svg">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <script>
      document.documentElement.classList.replace("no-js", "js")
    </script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,opsz,wght@0,18..144,300..900;1,18..144,300..900&display=swap" rel="stylesheet">
  </head>

  <body class="font-[Merriweather]">
    <%= render "partials/flash" %>

    <nav class="border-t border-zinc-200 bg-gray-50 py-4 text-xs text-gray-600">
      <div class="mx-auto max-w-7xl px-4 flex justify-center gap-6">
        <%= link_to "Home", root_path, class: "hover:text-gray-900 transition-colors" %>
        <span class="text-gray-500">•</span>
        <%= link_to "Imprint", imprint_path, class: "hover:text-gray-900 transition-colors" %>
        <span class="text-gray-500">•</span>
        <%= link_to "Privacy Policy", privacy_path, class: "hover:text-gray-900 transition-colors" %>
        <span class="text-gray-500">•</span>
        <%= link_to "Terms of Service", terms_of_service_path, class: "hover:text-gray-900 transition-colors" %>
      </div>
    </nav>

    <main class="container mx-auto mt-20 mb-12 px-5 flex text-black/85">
      <%= yield %>
    </main>
  </body>
</html>
